version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: citramascoweb-backend
    ports:
      - "4000:4000"
    env_file:
      - .env
    restart: unless-stopped
